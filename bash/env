# paths
PATH="/usr/bin:/bin:/usr/sbin:/sbin"

if [ -d "/usr/local" ]; then
  PATH="/usr/local/bin:/usr/local/sbin:$PATH"
fi

if [ -d $HOME/.bin ]; then
  PATH="$HOME/.bin:$PATH"
fi

MANPATH="/usr/share/man:/usr/local/share/man"

export PATH MANPATH

# editor
EDITOR='vim'
export EDITOR

export CLICOLOR=1

# rip
RIPDIR="$HOME/.rip"
if [ -d $RIPDIR ]; then
  export RIPDIR
  export RIPPROCESSES=4

  export RUBYLIB="$RIPDIR/active/lib:$RIPDIR/system/lib"
  export PATH="$RIPDIR/active/bin:$RIPDIR/system/bin:$PATH"

  eval `rip-config`
fi
